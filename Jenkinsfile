pipeline {			
    agent any
    stages {			
        stage('Build') {			
            steps {			
                echo 'DEVOLOP MASTER UPSTREAM executed'		
                     }			
        }			
   }		
}
